---
title: 'Indexierung'
description: 'Der RAG-Agent kann zahlreiche Quellen als Wissensbasis nutzen.'
---

In der Basis-Version stehen zwei Indexierungsmöglichkeiten zur Verfügung. Es können sowohl komplette Sitemaps als auch einzelne Seiten als URL eingetragen werden. Zusätzlich können dem Agent verschiedene Dateiformate als Wissensbasis zur Verfügung gestellt werden.

## Websites 

Es besteht die Möglichkeit, sowohl eine **Sitemap im XML-Format** als auch eine einfache **Website-URL** indexieren zu lassen. Die Sitemap-URL muss über das Internet öffentlich abrufbar sein.

<Tip>Wir empfehlen dir den Upload einer (Teil-)Sitemap im XML-Format. So kannst du gezielt Seiten ausschließen, musst jedoch nicht alle einzeln indexieren.</Tip>

### Sitemap

Der RAG-Agent kann die in der Sitemap-URL enthaltenen, einzelnen Seiten exportieren und als eigenen Eintrag in seine Wissensbasis zu importieren. Eine bereits vorhandene URL muss zuerst gelöscht werden, um sie erneut indexieren zu können.

Sobald die Indexierung der Seiten erfolgreich abgeschlossen wurde und der Agent auf das Wissen zugreifen kann, erhältst du eine Bestätigung E-Mail und der Status im Dashboard ändert sich von `Pending` in `Live`.

Die meisten Content-Management-Systeme (Bsp.: Typo3, WordPress, Drupal) sowie eCommerce-Plattformen (Bsp.: Shopware, Shopify, Magento) liefern standardmäßig entsprechende Sitemap-URLs mit.

Beispiel-Format für eine Produkt-Sitemap in Wordpress:
```
https://www.deinedomain.de/product-sitemap.xml
```

Wir empfehlen, zusätzlich zur Sitemap-URL auch einen Namen bzw. Beschreibungstext einzugeben, was dir die spätere Zuordnung einzelner Seiten zu einer bestimmten Sitemap erleichtert. Zudem kann die URL-Liste über ein Dropdown nach Sitemaps gefiltert werden.

### Einzelne URLs
---

## Dateien

<Tip>Der RAG-Agent versteht die Dateiformate **PDF, XLS/XLSX, TXT, DOC/DOCX** und *CSV*. Bei der Indexierung von Tabellen wird das Dateiformat `.csv` empfohlen.</Tip>

## Knowledge Bases (Add-On)
